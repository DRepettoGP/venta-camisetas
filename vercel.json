{
  "version": 2,
  "buildCommand": "npm run build:vercel",
  "installCommand": "npm install && npm install --prefix server",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
